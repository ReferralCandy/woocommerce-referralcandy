# Copy to .env (gitignored). Used only by `pnpm run zip:staging` (scripts/package.mjs).
# One line per define('WC_REFERRALCANDY_<X>_BASE', ...) in woocommerce-referralcandy.php.
WC_REFERRALCANDY_STAGING_API_BASE=https://staging.example.com/api/v1
WC_REFERRALCANDY_STAGING_MAIN_API_BASE=https://staging-mainapi.example.com/staging
WC_REFERRALCANDY_STAGING_APP_BASE=https://staging-app.example.com
